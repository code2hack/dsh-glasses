PLAN_SOURCE = CHATGPT

The worktree remains clean at the admitted base, so this is the implementation plan I want #27 to execute. The architectural rule for the ticket is:

Promote the proven TB0 one-session read path into the normative M1 attachment/snapshot model. Do not evolve the TB0 write slice.

Decisions locked for #27
DSH revision

For Ticket #27 and all new M1 evidence, the supported DSH revision is deliberately:

@deepseek-ai/dsh@0.1.1-rc.2
npm dist.integrity:
sha512-UP1UIh6q3Gme/yXRn/QL2P8IsVlv8Shpg22TRJIZPsCRWLm4CBiA1MUvXmJAfsOEETBMLAl+xWPtFw6ICsN3wg==

This is not “whatever dsh is on PATH.” The ticket deliberately advances the supported revision from TB0's historical 0.1.0-rc.7 to 0.1.1-rc.2, and then proves that revision.

The durable machine-readable pin should be:

plugins/dsh-glasses-plugin/dsh-compat.json

with approximately:

JSON
{
  "schemaVersion": 1,
  "package": "@deepseek-ai/dsh",
  "version": "0.1.1-rc.2",
  "npmDistIntegrity": "sha512-UP1UIh6q3Gme/yXRn/QL2P8IsVlv8Shpg22TRJIZPsCRWLm4CBiA1MUvXmJAfsOEETBMLAl+xWPtFw6ICsN3wg==",
  "requiredReadSeams": [
    "webServer.register",
    "sessionQuery.listSessions",
    "sessionQuery.readSession",
    "context.session/event",
    "agents.get"
  ]
}

The executable check lives at:

plugins/dsh-glasses-plugin/test/dsh-compat.test.mjs

It must fail closed on an exact version mismatch, npm dist-integrity mismatch, or disappearance/incompatible shape of any M1 read-side seam.

Do not put storage or apiProxy into the M1 compatibility contract. They belong to the dormant TB0/M3 write path. Keeping them in the M1 ABI would unnecessarily make a read-only slice depend on write infrastructure.

The old docs/evidence/tb0-dsh-compat-2026-08-19.md remains untouched historical evidence for rc.7. Do not rewrite it to say rc.2.

Normative M1 snapshot contract

For #27, /glasses/v1/bootstrap should return this shape. This fills in the currently-unspecified AttachmentProjection details without changing the semantic requirements of SPEC §§3/7.

JSON
{
  "protocolMajor": 1,
  "serverGeneration": "opaque-server-generation",
  "connectionEpoch": "opaque-connection-epoch",
  "attachmentSetRevision": 1,
  "streamSequence": 42,
  "attachments": [
    {
      "attachmentId": "opaque-random-attachment-id",
      "attachmentGeneration": 1,
      "sessionId": "selected-existing-dsh-session",
      "label": "Attached session",
      "order": 0,
      "state": "idle",

      "capabilities": {
        "historyRead": true,
        "liveUpdates": false,
        "draftMutations": false,
        "send": false,
        "steer": false,
        "interrupt": false,
        "resolveRequest": false
      },

      "agent": {
        "serverGeneration": "opaque-server-generation",
        "attachmentGeneration": 1,
        "state": "idle"
      },

      "history": {
        "serverGeneration": "opaque-server-generation",
        "attachmentGeneration": 1,
        "asOfSeq": 42,
        "events": []
      }
    }
  ],
  "drafts": []
}

The invariants are mandatory:

protocolMajor === 1

attachments.length === 1
drafts.length === 0

attachment.order === 0
attachment.sessionId === configured selected session

attachmentId is randomly generated/opaque
attachmentId MUST NOT encode sessionId

attachmentGeneration is a positive integer

attachment.state ∈
  idle | running | waiting-user | unavailable | unknown

attachment.agent.state === attachment.state

attachment.agent.serverGeneration === snapshot.serverGeneration
attachment.history.serverGeneration === snapshot.serverGeneration

attachment.agent.attachmentGeneration === attachment.attachmentGeneration
attachment.history.attachmentGeneration === attachment.attachmentGeneration

streamSequence === history.asOfSeq

history.events are strictly increasing by seq
history.events contain no duplicate seq
renderable block identity is stable and non-duplicated

all mutation capabilities === false

serverGeneration is generated once per plugin process/application instance. attachmentId is a fresh opaque UUID-like value for the attachment established by that plugin instance. attachmentGeneration is 1 for this static one-attachment M1 slice. Future attach/detach work can increment/recreate it.

connectionEpoch is generated fresh for each successful authenticated bootstrap.

attachmentSetRevision is 1 for this immutable one-attachment M1 set.

For M1, streamSequence is the bootstrap projection watermark and equals history.asOfSeq. This avoids inventing an independent sequence domain before live-delta work exists.

Canonical history projection

Do not send raw DSH payloads.

Continue using lib/projection.js, but give every renderable logical block a stable blockId.

Examples:

JSON
{
  "seq": 8,
  "type": "user/message",
  "blockId": "message:u-123",
  "message": {
    "role": "user",
    "id": "u-123",
    "text": "hello"
  }
}
JSON
{
  "seq": 15,
  "type": "assistant/message",
  "blockId": "message:a-456",
  "turn": 2,
  "step": 1,
  "message": {
    "role": "assistant",
    "id": "a-456",
    "text": "world"
  }
}

For an unfinished assistant block:

JSON
{
  "seq": 12,
  "type": "assistant/chunk",
  "blockId": "partial:2:1:0",
  "turn": 2,
  "step": 1,
  "chunk": {
    "type": "text-delta",
    "index": 0,
    "text": "wor"
  }
}

Use durable DSH message IDs where available. Deterministic seq-based fallback identity is acceptable only when DSH provides no message ID.

Do not expose TB0's rpcId, storage information, raw event payloads, provider URLs, or unrestricted DSH metadata merely because the old projection happened to have them.

Ordered implementation plan
T27-01 — Establish the rc.2 compatibility contract

Description: Add the machine-readable supported DSH pin and executable compatibility checker before changing the protocol.

Deliverables

plugins/dsh-glasses-plugin/dsh-compat.json
plugins/dsh-glasses-plugin/test/dsh-compat.test.mjs

The checker must establish:

exact @deepseek-ai/dsh@0.1.1-rc.2;

exact pinned npm dist.integrity;

webServer.register;

sessionQuery.listSessions;

sessionQuery.readSession;

subscription/disposal of session/event;

agents.get;

no M1 dependency on storage/apiProxy.

The dist-integrity leg may query npm registry during qualification, but the pin itself must be committed. Runtime seam/version checking must use the actual DSH_BIN being qualified.

Command

Bash
DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-compat.test.mjs

Expected: exit 0 and explicit PASS for revision, integrity, and every required seam.

Acceptance: AC6.

Risk: HIGH. If rc.2 differs materially at a required read seam, stop here and checkpoint me. Do not build compatibility shims speculatively.

T27-02 — Introduce the project-owned DSH adapter

Description: Add the SPEC §5 isolation boundary and remove the M1 read path's direct dependence on DSH internals.

Deliverables

plugins/dsh-glasses-plugin/lib/dsh-adapter.js
plugins/dsh-glasses-plugin/test/dsh-adapter.test.mjs

Implement the M1/read portion of GlassesDshAdapter:

listAttachableSessions()
readProjectionPage(sessionId, cursor?)
observeSession(sessionId, listener)
getAgentState(sessionId)

For #27:

cursor may be absent only; paging itself is later work.

readProjectionPage() performs bounded reads and emits canonical projections.

observeSession() strictly filters the configured session and returns a disposer. It exists primarily to move the existing TB0 stream seam behind the adapter; SSE behavior is not being developed by #27.

getAgentState() maps:

DSH idle → idle

DSH running → running

missing agent → unavailable

unrecognized future status → unknown

listAttachableSessions() is internal only. Nothing from that list is sent to glasses except the explicitly configured selected session.

Runtime validation should reject malformed/non-monotonic projection pages rather than silently normalize them.

Commands

Bash
node plugins/dsh-glasses-plugin/test/dsh-adapter.test.mjs

DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-adapter-runtime.test.mjs

The runtime test must boot a disposable rc.2 instance and exercise the real adapter against it.

Acceptance: AC2, AC6.

Risk: HIGH. This is the upstream isolation boundary; do not leak ctx.sessionQuery, ctx.agents, or ctx.on('session/event') back into the new bootstrap implementation.

T27-03 — Canonicalize history ordering and block identity

Description: Harden the existing projection.js; do not replace the projection architecture.

Change

plugins/dsh-glasses-plugin/lib/projection.js
plugins/dsh-glasses-plugin/test/projection.test.mjs
apps/glasses-android/app/src/main/assets/c0-core.js
apps/glasses-android/test/c0-core.test.mjs

Preserve the current event-folding approach, but:

output only canonical fields;

attach stable blockId to renderable content;

preserve DSH seq ordering;

reject/flag duplicate or backwards sequence data before snapshot creation;

use blockId as the preferred conversation identity;

retain final-message replacement of partial assistant output;

ensure replaying one snapshot cannot render a finalized assistant answer once as partial and again as final.

Commands

Bash
node plugins/dsh-glasses-plugin/test/projection.test.mjs
node apps/glasses-android/test/c0-core.test.mjs

Add an explicit replay fixture that passes the same canonical history through the reducer twice from reset and obtains exactly the same ordered block identities.

Acceptance: AC2, AC4.

Risk: MEDIUM-HIGH. The dangerous failure here is visually duplicated assistant content from chunk + final-message folding.

T27-04 — Replace ad-hoc bootstrap with the canonical snapshot

Description: Make the server produce the exact M1 snapshot contract above.

Change

plugins/dsh-glasses-plugin/lib/index.js

Recommended new pure helper:

plugins/dsh-glasses-plugin/lib/snapshot.js

Responsibilities:

one configured session only;

opaque attachment ID;

attachment generation;

label;

exact attachment state;

read-only capabilities;

separate agent projection;

bounded canonical history;

connection epoch;

attachment-set revision;

stream sequence;

drafts: [].

Set and enforce a hard maximum for bootstrapMaxEvents; do not allow an arbitrary config value to remove the bound.

The bootstrap path should only call the adapter and snapshot builder.

Write quarantine

For ordinary M1 plugin startup:

/glasses/v1/draft/mutations  -> NOT REGISTERED
/glasses/v1/actions          -> NOT REGISTERED

They therefore fall into the /glasses/v1 fallback and return 404.

Do not delete their TB0 implementation.

Do not use draft state, reconciliation, storage, or apiProxy while constructing M1 bootstrap.

/glasses/v1/stream may remain as pre-existing TB0 compatibility code, but #27 must neither evolve it nor advertise/use it as an M1 capability. Client liveUpdates is false.

Command

Bash
node plugins/dsh-glasses-plugin/test/snapshot-contract.test.mjs

Acceptance: AC1, AC2, AC5.

Risk: HIGH. This is the wire-format break from TB0's attachment/history/draft/writeState response.

T27-05 — Add exhaustive server/protocol snapshot tests

Description: Freeze the M1 wire contract before wiring the client to it.

Deliverables

plugins/dsh-glasses-plugin/test/snapshot-contract.test.mjs

Positive fixture must prove a complete snapshot.

Negative fixtures should include at minimum:

unsupported protocolMajor
missing serverGeneration
missing connectionEpoch
zero attachments
two attachments
wrong configured sessionId
missing/empty attachmentId
non-positive attachmentGeneration
invalid attachment state
non-zero attachment order
historyRead != true
any mutation capability == true
non-empty drafts[]
agent state != attachment state
agent wrong serverGeneration
history wrong serverGeneration
agent wrong attachmentGeneration
history wrong attachmentGeneration
history.events not an array
descending event sequence
duplicate sequence
event seq > asOfSeq
duplicate render blockId
malformed projected message/chunk
streamSequence != history.asOfSeq
history beyond configured maximum

Also explicitly test:

attachmentId !== sessionId
!attachmentId.includes(sessionId)

for server-generated attachments.

Command

Bash
node plugins/dsh-glasses-plugin/test/snapshot-contract.test.mjs

Acceptance: AC1, AC2, AC3, AC4, AC5.

Risk: MEDIUM.

T27-06 — Add a pure atomic snapshot staging module to the client

Description: Snapshot validation/staging must happen without touching DOM or installed client state.

Deliverable

apps/glasses-android/app/src/main/assets/snapshot-core.js
apps/glasses-android/test/snapshot-core.test.mjs

Expose a pure API along these lines:

stageSnapshot(rawSnapshot, {
  protocolMajor: 1,
  expectedSessionId,
  expectedServerGeneration?
})
    -> { ok: true, snapshot: stagedSnapshot }
    -> { ok: false, code: ... }

The implementation must:

Validate the complete outer envelope.

Validate one attachment.

Validate all generation relationships.

Validate read-only capabilities.

Validate drafts=[].

Validate history sequence and block identities.

Build a new detached conversation state.

Return the detached staged result.

Mutate absolutely nothing on failure.

Do not sort malformed history into correctness. If the wire events are backwards or duplicated, reject the snapshot.

This distinction matters:

bad:
receive -> mutate global state -> discover error

required:
receive -> validate -> build detached staged state
        -> success? single install
        -> failure? discard entire staged object

Commands

Bash
node apps/glasses-android/test/snapshot-core.test.mjs
node apps/glasses-android/test/c0-core.test.mjs

Test that globals/previous staged state remain byte-for-byte logically unchanged after every negative fixture.

Acceptance: AC3, AC4, AC5.

Risk: HIGH. Atomicity must be a property of the code structure, not just a test that happens to pass.

T27-07 — Wire app.js to stage → install and make M1 strictly read-only

Change

apps/glasses-android/app/src/main/assets/index.html
apps/glasses-android/app/src/main/assets/app.js

Add snapshot-core.js before app.js.

Change bootstrap flow to:

fetch raw
→ stageSnapshot(...)
→ if rejected: keep previous visible state unchanged
→ if accepted: installSnapshot(staged) synchronously

installSnapshot() is the only function allowed to replace:

server generation
attachment identity/generation
session state
history watermark
conversation state
diagnostic metadata
rendered chat

For #27, disable the client write path completely:

do not enter Input mode;

composer remains hidden;

no paste;

no Send;

no mutation retry;

no send retry;

no resume of pending TB0 mutation/send records;

no POST to /draft/mutations;

no POST to /actions;

no automatic SSE opening for this ticket.

The old functions may remain in source temporarily as dormant TB0/M3 code; they must have zero reachable call path in the M1 client.

Do not clear old pending localStorage records merely to make the test pass. Ignore them. M3 can decide their migration semantics.

On a rejected snapshot:

existing valid screen stays unchanged
no partial new state appears
no writable state becomes enabled
trace contains bounded rejection reason

For an initial bootstrap with no previous valid screen, remain in non-session/configuration/error presentation.

Commands

Bash
node apps/glasses-android/test/snapshot-core.test.mjs
node apps/glasses-android/test/c0-core.test.mjs

Acceptance: AC3, AC5.

Risk: HIGH. Search the completed code for every caller of startSend, pasteClipboard, mutation retry, and send retry before declaring this complete.

T27-08 — Add a real DOM render assertion using jsdom

For Spark Node v24.14.1, use jsdom 29.1.1.

Do not use Playwright MCP: it is an external interactive/project tool rather than a committed deterministic test dependency.

Do not use a hand-written DOM shim: that risks validating the shim instead of the production HTML/JS.

Do not add Chromium/Puppeteer merely for this ticket.

Deliverables

apps/glasses-android/package.json
apps/glasses-android/package-lock.json
apps/glasses-android/test/m1-render.test.mjs

Pin:

JSON
"jsdom": "29.1.1"

The harness should load the actual committed:

index.html
c0-core.js
snapshot-core.js
app.js

and provide a fake GlassesBridge.

The fake bridge must record every native call.

Required assertions:

one valid bootstrap renders the expected user/assistant text;

exact expected number of rendered chat blocks;

stable blockIds;

each expected message appears exactly once;

second complete resnapshot replaces/reconstructs rather than duplicates;

malformed snapshot does not alter existing rendered chat;

unsupported major does not alter existing rendered chat;

wrong generation does not alter existing rendered chat;

composer remains hidden;

no write control becomes visible;

bridge sees no /draft/mutations;

bridge sees no /actions;

bridge sees no openStream() call.

Commands

Bash
npm --prefix apps/glasses-android ci
node apps/glasses-android/test/m1-render.test.mjs

Acceptance: AC3, AC4, AC5.

Risk: MEDIUM.

T27-09 — Disposable rc.2 adapter + narrow-edge integration proof

This is the strongest #27 runtime gate.

Deliverables

plugins/dsh-glasses-plugin/test/dsh-adapter-runtime.test.mjs
plugins/dsh-glasses-plugin/test/m1-narrow-edge.test.mjs

Use direct disposable dsh spawning in the style already proven by host-write-recovery.test.mjs.

Do not use dev/d0-runtime.mjs for this ticket's qualification.

Create at least:

session A = selected/attached
session B = unrelated sentinel session

Give them distinguishable synthetic content.

Then prove through the normal plugin+narrow proxy:

unauthenticated bootstrap -> 401

authenticated bootstrap -> 200

attachments.length === 1

attachments[0].sessionId === session A

serialized bootstrap contains:
  selected-session sentinel
  NOT session-B id
  NOT session-B label/content

history length <= configured bound

/api/status through narrow proxy -> 403
/api/session.list through narrow proxy -> 403
/api/session.prompt through narrow proxy -> 403

/glasses/v1/draft/mutations -> 404
/glasses/v1/actions -> 404

unknown /glasses/v1/* -> 404

agent projection is present

all advertised mutation capabilities === false

Then feed the actual HTTP bootstrap response into the same snapshot-core staging logic and render harness. Do not validate server and client only with separate hand-written fixtures.

That gives the desired chain:

real disposable DSH
→ real adapter
→ real plugin bootstrap
→ real narrow proxy
→ production client validator
→ production client reducer
→ production DOM

Expected synthetic content must render exactly once.

Command

Bash
DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-adapter-runtime.test.mjs

DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/m1-narrow-edge.test.mjs

No vLLM dependency should be introduced.

Acceptance: AC1, AC2, AC4, AC5, AC6.

Risk: HIGH. Any appearance of the unrelated session identifier/content in the glasses response is a ticket-blocking security failure.

T27-10 — Preserve but quarantine TB0 compatibility artifacts

This is an explicit reconciliation item because the old implementation conflicts with M1.

Do not delete:

plugins/dsh-glasses-plugin/test/host-write-recovery.test.mjs
existing TB0 write-state implementation
existing TB0 evidence
existing SSE implementation
existing c0 reducer coverage

But none of those writes may be reachable from the ordinary M1 runtime/client.

The legacy write recovery suite is not an M1 acceptance gate. Do not distort #27 merely to keep the old write-enabled public surface active.

Also:

docs/evidence/tb0-dsh-compat-2026-08-19.md

stays historical and untouched.

dev/d0-runtime.mjs retains its historical:

EXPECTED_DSH_VERSION = 0.1.0-rc.7
vLLM defaults

Do not change that pin to rc.2 as part of #27. The #27 rc.2 runtime harness is deliberately separate.

If its old bootstrap field parser is now incompatible with the new response, document that it is a TB0 legacy harness; do not expand #27 into a D0 redesign.

Validation

Bash
git diff -- docs/evidence/tb0-dsh-compat-2026-08-19.md
git diff -- dev/d0-runtime.mjs

Expected for this ticket: preferably empty for both.

Also search production entry points for reachable M1 writes.

Bash
rg -n "draft/mutations|/actions|startSend|pasteClipboard|resumePendingOperations" \\
  plugins/dsh-glasses-plugin/lib \\
  apps/glasses-android/app/src/main/assets

Review every hit and prove it is dormant/nonregistered.

Acceptance: AC5, AC6 plus scope discipline.

Risk: MEDIUM.

T27-11 — Build the debug APK

No device installation is required or allowed as acceptance evidence.

Command

Bash
ANDROID_HOME=/home/code2hack/Android/Sdk \\
gradle -p apps/glasses-android assembleDebug

Expected artifact:

apps/glasses-android/app/build/outputs/apk/debug/app-debug.apk

Then record:

Bash
stat -c '%s' apps/glasses-android/app/build/outputs/apk/debug/app-debug.apk
sha256sum apps/glasses-android/app/build/outputs/apk/debug/app-debug.apk

The APK remains gitignored.

Do not commit it.

Acceptance: required validation gate.

Risk: LOW-MEDIUM.

T27-12 — Run the complete acceptance gate and commit durable evidence

Run from a clean candidate tree after implementation tests are stable:

Bash
DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-compat.test.mjs

node plugins/dsh-glasses-plugin/test/projection.test.mjs
node plugins/dsh-glasses-plugin/test/snapshot-contract.test.mjs

DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-adapter.test.mjs

DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/dsh-adapter-runtime.test.mjs

DSH_BIN=/home/code2hack/.npm-global/bin/dsh \\
node plugins/dsh-glasses-plugin/test/m1-narrow-edge.test.mjs

node apps/glasses-android/test/c0-core.test.mjs
node apps/glasses-android/test/snapshot-core.test.mjs

npm --prefix apps/glasses-android ci
node apps/glasses-android/test/m1-render.test.mjs

ANDROID_HOME=/home/code2hack/Android/Sdk \\
gradle -p apps/glasses-android assembleDebug

Then evidence goes under:

docs/evidence/m1-ticket-27-atomic-snapshot-2026-08-22.json
docs/evidence/m1-ticket-27-atomic-snapshot-2026-08-22.md

The JSON is the machine-readable source; Markdown explains it.

Required JSON structure:

JSON
{
  "schemaVersion": 1,
  "repo": "code2hack/dsh-glasses",
  "milestone": "M1",
  "ticket": 27,
  "base": "e770e4d39b0df32ce7ee5d8cb7f5c914463fca94",
  "head": "<exact-tested-head>",
  "branch": "workflow/ticket-27",

  "dsh": {
    "package": "@deepseek-ai/dsh",
    "version": "0.1.1-rc.2",
    "npmDistIntegrity": "sha512-UP1UIh6q3Gme/yXRn/QL2P8IsVlv8Shpg22TRJIZPsCRWLm4CBiA1MUvXmJAfsOEETBMLAl+xWPtFw6ICsN3wg==",
    "compatibility": "PASS"
  },

  "runtime": {
    "kind": "fresh-disposable",
    "host": "spark",
    "providerRequired": false,
    "selectedSessions": 1,
    "unrelatedSyntheticSessions": 1
  },

  "apk": {
    "path": "apps/glasses-android/app/build/outputs/apk/debug/app-debug.apk",
    "sizeBytes": 0,
    "sha256": "<actual>"
  },

  "scenarios": [
    {
      "id": "snapshot-complete",
      "result": "PASS",
      "assertions": []
    }
  ],

  "negativeAssertions": [
    "unsupported protocol major was not installed",
    "incomplete snapshot was not installed",
    "malformed snapshot was not installed",
    "wrong-generation snapshot was not installed",
    "unrelated DSH session was not exposed",
    "unrestricted DSH API was not exposed",
    "draft mutation endpoint was unavailable",
    "action endpoint was unavailable",
    "no client write request was emitted",
    "expected rendered content appeared exactly once"
  ],

  "provenance": {
    "synthetic": true,
    "physicalDeviceUsed": false,
    "physicalInputUsed": false,
    "manualVisualJudgmentUsed": false,
    "physicalHardwareBehaviorClaimed": false
  }
}

Obviously sizeBytes, head, hashes, timestamps, and scenario results must contain actual measured values, not placeholders.

No bearer token, disposable session ID, private content, or provider credentials go into evidence.

Acceptance: all ACs.

Risk: MEDIUM. The evidence must refer to the exact tested head, not an earlier commit.

T27-13 — Freeze candidate, push, and request exact-head review

Once T27-12 passes:

Bash
git status --short
git diff --check
git rev-parse HEAD
git log -1 --oneline

Commit/push the exact candidate according to AGENTS.md provenance rules.

Then report to me with:

kind: review
ticket: #27
base: e770e4d39b0df32ce7ee5d8cb7f5c914463fca94
head: <exact candidate SHA>
branch: workflow/ticket-27
evidence: docs/evidence/m1-ticket-27-atomic-snapshot-2026-08-22.{json,md}

I will do the ChatGPT-first exact-head final review required by AGENTS.md §14.

Do not merge unless separately authorized.

Acceptance matrix
Acceptance criterion	Primary plan items
AC1 exactly one attachment, opaque identity/generation/label/state/capabilities	T27-04, T27-05, T27-09
AC2 authenticated bounded history + agent state, no unrelated exposure	T27-02, T27-03, T27-04, T27-09
AC3 complete atomic staging; reject incomplete/malformed/wrong-generation/unsupported-major	T27-05, T27-06, T27-07, T27-08
AC4 stable ordering/block identity; disposable runtime renders exactly once	T27-03, T27-06, T27-08, T27-09
AC5 mutation actions disabled	T27-04, T27-05, T27-07, T27-08, T27-09, T27-10
AC6 DSH revision and adapter assumptions pinned/executable	T27-01, T27-02, T27-09
Files that must not be redesigned by #27

Keep these boundaries firm:

DO NOT rewrite or falsify historical TB0 evidence.
DO NOT delete the TB0 write implementation.
DO NOT delete host-write-recovery.test.mjs.
DO NOT promote TB0 writes into M1.
DO NOT change dev/d0-runtime.mjs's rc.7 historical pin/provider design.
DO NOT implement live-delta/SSE semantics.
DO NOT implement history-reading behavior.
DO NOT implement multiple tabs.
DO NOT implement drafts.
DO NOT implement Send/Steer/Interrupt/request resolution.
DO NOT modify physical input mappings.
DO NOT add real-device/manual-visual gates.
DO NOT expose session.list or arbitrary /api/* through the glasses edge.
DO NOT make storage/apiProxy part of the M1 supported DSH ABI.
DO NOT package/sign/publish a release APK.

The intended end state is therefore very narrow:

one selected DSH session
        ↓
rc.2 GlassesDshAdapter
        ↓
bounded canonical projection
        ↓
one canonical GlassesSnapshot
        ↓
validate + stage off-screen
        ↓
atomic install
        ↓
exactly-once read-only rendering

