# TB0-I0 — Rokid physical input qualification evidence

**Status:** automatable qualification in progress; physical controls remain unqualified until a genuine hardware trace is captured.  
**Branch:** `tb0/input-qualification`.  
**Base:** `6d1e1925b967cda3c19731decc570d02da2c9c6d` (`main`, including final G0 regression evidence).  
**Target device:** serial `1906092617103125`, firmware fingerprint `Rokid/glasses/glasses:12/SKQ1.240613.001/1.23.009-20260725-150201:user/release-keys`.

## Evidence provenance labels

Every input claim MUST carry exactly one provenance label:

- `PHYSICAL` — generated by a genuine Rokid hardware interaction while the current `dsh-glasses` APK and synchronized low-level/framework tracers were armed.
- `SYNTHETIC_ADB` — generated through Android `adb shell input` or another framework-level injection.
- `SYNTHETIC_SENDEVENT` — generated by writing Linux input events through a debug-only injection path. This proves reducer/wiring behavior, not physical hardware behavior.
- `PRIOR_REFERENCE` — evidence from official Rokid documentation or Poker-Dealer records on the same firmware/device family. It may inform hypotheses but cannot qualify this APK.

Only `PHYSICAL` evidence can close a physical-control row.

## Capture topology

```text
Spark worker
  -> SSH u4090
  -> tmux dsh-glasses-adb
  -> /opt/android-sdk/platform-tools/adb
  -> USB Rokid serial 1906092617103125
```

Product traffic remains Rokid ↔ Spark over Tailscale/private LAN. ADB is diagnostics only.

## Required synchronized channels

For each bounded capture window, preserve:

1. `/dev/input/event0` and `/dev/input/event1` through `getevent -lt`;
2. `DSHGlasses`/`DSHGlassesBridge` framework and lifecycle logs;
3. relevant `InputReader`, `InputDispatcher`, Activity/window-focus, and Rokid service logs;
4. foreground Activity and focused-window samples;
5. before/after screenshot and UI hierarchy when a native side effect is suspected;
6. monotonic timestamps and a capture manifest tying every file to commit/APK/device identity.

Do not clear retained logs before inspecting them.

## Device inventory

| Item | Result | Provenance |
|---|---|---|
| `/dev/input/event0` | `qpnp_pon`; KEY_VOLUMEDOWN, KEY_MENU | `SYNTHETIC_ADB` inventory |
| `/dev/input/event1` | `ROKID,PSOC-TP-R`; ENTER, directional keys, PROG1/2/3, F13/F14, BACK, DASHBOARD | `SYNTHETIC_ADB` inventory |
| Android input devices | three devices; PSOC touch/key source present | `SYNTHETIC_ADB` inventory |
| Head-pose sensors | Game Rotation Vector, gyro, accelerometer available | `SYNTHETIC_ADB` inventory |

## Qualification matrix

| Control candidate | Current APK physical trace | Synthetic path | Prior same-firmware evidence | Native side effect | Status |
|---|---|---|---|---|---|
| function-button short | pending | pending/available | pending | unknown | **unqualified** |
| function-button long | pending | pending/available | pending | unknown | **unqualified** |
| one-finger touch | pending | pending/available | pending | unknown | **unqualified** |
| one-finger swipe forward/backward | pending | pending/available | pending | unknown | **unqualified** |
| two-finger touch | pending | pending/available | pending | unknown | **unqualified** |
| two-finger swipe forward/backward | pending | pending/available | pending | unknown | **unqualified** |
| head pose | no physical gesture required for sensor availability; dynamic replay pending | sensor/replay pending | n/a | n/a | **partially qualified** |

## Per-interaction trace template

### `<interaction name>`

- Provenance: `PHYSICAL | SYNTHETIC_ADB | SYNTHETIC_SENDEVENT | PRIOR_REFERENCE`
- Source commit/APK:
- Capture start/end, monotonic and wall-clock:
- Linux device/event sequence:
- Android key/touch/motion callbacks:
- Down/up/repeat/cancel timing:
- Device ID/name, keycode, scan code, source, flags:
- Pointer IDs/coordinates/pressure/tool type, if applicable:
- Activity/window/focus effects:
- Separately observed Rokid-native behavior:
- Interpretation:
- Remaining uncertainty:

## Acceptance rules

A physical binding may be frozen only when a genuine interaction proves:

- complete BEGIN/UPDATE-or-HOLD/END-or-CANCEL lifecycle;
- stable raw event identity and Android mapping;
- timing sufficient to distinguish short, double, and long behavior where required;
- focus/background cancellation behavior;
- whether a conflicting Rokid-native operation also fires;
- reproducibility across at least three bounded trials without unexplained divergence.

Synthetic replay is accepted only for validating tracer/reducer plumbing. It must never be presented as hardware qualification.

## Passive capture state

Record the active u4090 tmux session, output directory, recorder PID(s), rotation policy, and last health check here. Keep the passive recorder armed after automatable work is exhausted so an incidental real interaction can be captured without interrupting `code2hack`.

## Remaining physical-only gap

Until genuine physical interactions occur, keep these explicit and open:

- function-button short press;
- function-button long press;
- one- and two-finger touch/swipe mappings;
- native Rokid conflict/exclusivity for each mapping.

The project may continue with a debug semantic-control injection seam while these physical bindings remain unqualified.
