# M1 #28 acceptance evidence

Ticket #28 passes AC1–AC6 at frozen production/test head
`9de5480f851aba5cec9f7893ff2a8c499119f9ca`, admitted from
`6ac06af4a06f1547d26bfe822d6a5b81aef8c64b` on
`workflow/ticket-28`. The machine-readable source of truth is
`m1-28-acceptance-2026-08-22.json`.

## Result

- AC1: fresh epochs, exact hello bases, monotonic stream sequence, and stale
  epoch rejection pass.
- AC2: the complete canonical projection vocabulary and exact-once live
  finalized output pass.
- AC3: following pinning and history-reading anchor/unread behavior pass.
- AC4: real authenticated older-history paging prepends without reorder,
  duplication, anchor loss, watermark advance, or unread change.
- AC5: every required stream/protocol fault stops live transport, leaves writes
  disabled, retains the last valid screen, and converges through a complete
  fresh-snapshot resynchronization.
- AC6: real disconnect/reconnect and rc.2 process restart reconstruct committed
  history exactly once without semantic replay.

The final gate included all server/client contract suites, 16 fault scenarios,
the fresh-home rc.2 adapter runtime, the fresh-home live/disconnect/restart
runtime, the full real DSH → plugin → proxy → shipped-client narrow edge, proxy
isolation, and the debug APK qualification. The DSH compatibility guard passed
for `@deepseek-ai/dsh@0.1.1-rc.2` with the registry integrity recorded in the
JSON evidence.

## Runtime and isolation

Every DSH runtime suite used a separately created explicit disposable
`DSH_HOME` outside the shared user profile, dedicated ports/processes, and
automated cleanup. The shared profile was never used as a fixture, source,
fallback, or cleanup target. Runtime evidence contains only synthetic content.

The Android application was exercised through a synthetic native-bridge
boundary around the shipped `app.js`, `snapshot-core.js`, and `c0-core.js`.
No physical input, manual judgment, or human gate was used.

## Scope boundary

This is debug-build M1 acceptance. It does not claim a release build,
publication/signing qualification, or physical Rokid behavior. Multiple
attachments and every write operation remain out of scope and unreachable.
